name: Payment Service CI

on:
  push:
    branches: [develop, main]
    paths:
      - "services/order-services/payment-service/**"
      - ".github/workflows/payment-service.yml"
  pull_request:
    branches: [develop, main]
    paths:
      - "services/order-services/payment-service/**"
      - ".github/workflows/payment-service.yml"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"
          cache: maven

      - name: Gitleaks scan
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_CONFIG: .gitleaks.toml

      - name: Install parent POM
        run: mvn install -N -DskipTests

      - name: Install shared modules
        run: mvn install -pl shared/common-dto,shared/common-exceptions -DskipTests

      - name: Build payment-service
        run: mvn -pl services/order-services/payment-service clean package -DskipTests

      - name: Build Docker image
        run: |
          docker build -t payment-service:${{ github.sha }} \
            services/order-services/payment-service/

      - name: Trivy vulnerability scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: payment-service:${{ github.sha }}
          format: table
          exit-code: "1"
          severity: CRITICAL
